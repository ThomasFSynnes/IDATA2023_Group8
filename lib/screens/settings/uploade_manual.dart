import 'package:flutter/material.dart';

class UploadeManual extends StatelessWidget{
  const UploadeManual({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Login"),
      ),
      body: const Column(children: [
        Text("Todo: implement upload manual"),
      ]),
    );
  }
}